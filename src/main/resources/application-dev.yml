server:
  port: 8004

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tzms
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database:
    database-platform: